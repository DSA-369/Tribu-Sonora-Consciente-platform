services:
  app:
    build: .
    container_name: tribu_sonora_app
    restart: always
    ports:
      - "3000:3000"
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - BACKEND_PORT=8000